const path = require('path'); // CJS 방식 import

module.exports = {
    # resolve() : 상대경로를 절대경로로 반환하는 함수
    # databases/config/sequelize-cli.config.js 파일을 Sequelize 설정으로 사용
    config: path.resolve('databases', 'configs', 'sequelize-cli.config.js'),
    # 모델 파일을 app/models 폴더에서 관리
    'models-path': path.resolve('app', 'models'),
    # 시더 파일을 databases/seeders 폴더에서 관리
    'seeders-path': path.resolve('databases', 'seeders'),
    # 마이그레이션 파일을 databases/migrations 폴더에서 관리
    'migrations-path': path.resolve('databases', 'migrations'),
};